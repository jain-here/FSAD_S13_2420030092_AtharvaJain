

# FSAD_S13_2420030092_AtharvaJain

## Week 2: Hibernate CRUD
- Implemented Product entity and CRUD operations using Hibernate and MySQL.
- All CRUD operations tested and working.

## Week 3: Hibernate HQL (Sorting, Pagination, Aggregates)
- Added 5-8 products for HQL queries.
- Implemented HQL queries for:
  - Sorting by price (asc/desc)
  - Sorting by quantity (desc)
  - Pagination (first 3, next 3)
  - Aggregate functions (count, min, max, group by)
  - Filtering by price range
  - LIKE queries (starts with, ends with, contains, exact length)
- All outputs visible in console after running Main.java in Week3.


## Week 4: Spring Boot DI Project
- Created a Spring Boot project demonstrating Dependency Injection (DI) using constructor and setter injection.
- Defined a `Student` bean with properties injected from `application.properties`.
- Used `CommandLineRunner` to display student details at application startup.
- Configured application to run on port 8085.
- Project files in `Week4/studentdi/`.

## Week 5: Spring @Autowired Demo
- Developed a Spring project to demonstrate the use of `@Autowired` for dependency injection.
- Created `Student` and `Certification` beans, with `Certification` injected into `Student` using `@Autowired` annotation.
- Used Java-based configuration with `@Configuration` and `@ComponentScan`.
- Displayed student and certification details using a main class.
- Project files in `Week5/AutowiredDemo/`.
